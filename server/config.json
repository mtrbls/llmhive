{
  "server_url": "http://localhost:8000",
  "operator_url": "http://localhost:8000",
  "server_port": 8000,
  "node_port": 8001,
  "health_check_interval": 30,
  "health_check_timeout": 5,
  "poll_interval": 2,
  "pricing": {
    "price_per_token": 0.0001,
    "currency": "CCD",
    "description": "Price per token in CCD (can use PLT stablecoins like USDR/EURR with same decimal precision)"
  },
  "database": {
    "url": "sqlite:///data/odla.db"
  }
}
